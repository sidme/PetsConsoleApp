namespace PetsConsoleApp.Model.Enum
{
    /// <summary>
    /// Enum to represent types of pet application can hold.
    /// </summary>
    public enum PetType
    {
        Cat,
        Dog,
        Fish
    }
}
